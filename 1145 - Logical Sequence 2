#include <stdio.h>
int main()
{
    int Y,X,b,i,j,k,l,A=1;
    scanf("%d %d",&X,&Y);
    k=Y/X;
    l=X;
    for(i=1;i<=k;i++)
	{
		printf("%d",A);
		for(j=A+1;j<=l;j++)
		{
			printf(" %d",j);
		}
		printf("\n");
		A=A+X;
		l=l+X;
	}

    return 0;
}
