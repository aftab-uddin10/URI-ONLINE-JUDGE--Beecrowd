#include <stdio.h>
#include <string.h>

int main() {
    char A[500];
    scanf("%[^\n\r]", A);

    if (strlen(A) <= 80) {
        printf("YES\n");
    } else {
        printf("NO\n");
    }

    return 0;
}
