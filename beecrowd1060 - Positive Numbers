#include <stdio.h>

int main() {
    double num;
    int positiveCount = 0;

    for (int i = 0; i < 6; ++i) {
        scanf("%lf", &num);
        if (num > 0) {
            positiveCount++;
        }
}
    printf("%d valores positivos\n", positiveCount);

    return 0;
}
