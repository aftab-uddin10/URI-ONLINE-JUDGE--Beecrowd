#include <stdio.h>
#include <string.h>

int main ()
{

	char A[25], B[25];
	scanf("%s %s", A,B);

	if (strcmp(A,B) > 0)
	{	
		printf("%s\n", B);
		printf("%s\n", A);
	}
	else if (strcmp(A, B) < 0)
	{
		printf("%s\n", A);
		printf("%s\n", B);
	}
}
