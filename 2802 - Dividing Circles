#include <stdio.h>
#include <math.h>
 
int main() { 
    double N, A;

    scanf("%lf", &N);
	A = (pow(N, 4.0) - (6.0 * pow(N, 3.0)) + (23.0 * pow(N, 2.0)) - (18.0 * N) + 24.0 ) / 24.0;
    printf("%.0lf\n", A);

    return 0;
}
