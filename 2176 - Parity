#include <stdio.h>
#include <string.h>
int main()
{
    char A[150];
    int i, c=0;
    scanf("%s", A);
    for(i=0;A[i]; i++)
        if(A[i]=='1')
            c++;
    if(c%2 == 0)
        A[i] = '0';
    else
        A[i] = '1';
    A[i+1] = '\0';
    printf("%s\n", A);
    return 0;
}
