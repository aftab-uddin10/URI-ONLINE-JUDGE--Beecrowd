#include <stdio.h>
int main()
{
   int N[20], A,i,j;
    for(i=0; i<20; i++)
        scanf("%d",&N[i]);
        for(i=0,j=19; i<10; i++,j--)
    {
        A=N[i];
        N[i]=N[j];
        N[j]=A;
    }
    for(i=0; i<20; i++)
        printf("N[%d] = %d\n",i,N[i]);
    return 0;
}
