#include <stdio.h>
 
int main() {
 int M,N,i,j,sum,s,x;
 for(x=0;x<3;x++){
 scanf("%d %d",&M,&N);
 if(M>0 && N>0 ){
     if(M>N){
         sum=0;
         for(i=N;i<=M;i++){
         printf("%d ",i);  
          sum=sum+i; }
     
       printf("Sum=%d\n",sum); }
     if(N>M){ 
         s=0;
         for(j=M;j<=N;j++){
            printf("%d ",j);
               s=s+j; }
     
       printf("Sum=%d\n",s); }
 }
 }
 
 
    return 0;
}
