#include <stdio.h>
 
int main() {
 
double num,sum;
    int positiveCount = 0;

    for (int i = 0; i < 6; ++i) {
        scanf("%lf", &num);
        if (num > 0) {
            positiveCount++;
           sum = sum + num ;
        }
}   
    double p = positiveCount ;
   double avg = sum / p;
    printf("%d valores positivos\n", positiveCount);
     printf("%.1lf\n",avg );
 
    return 0;
}
