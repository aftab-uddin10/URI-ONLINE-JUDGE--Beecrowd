#include <stdio.h>
 
int main() {
 int N[10],i;
 scanf("%d",&N[0]);
 if(N[0] < 50){
      printf("N[0] = %d\n",N[0]);
     for(i=1;i<10;i++){
         N[0]=2*N[0];
         printf("N[%d] = %d\n",i,N[0]);
     }
 }
    return 0;
}
